{
  "name": "404page",
  "version": "1.0.0",
  "description": "Ju 404 Page",
  "scripts": {
    "start": "parcel src/index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build src/index.html --no-source-maps --public-url ./",
    "build": "npm run clean && npm run build:parcel"
  },
  "keywords": [],
  "author": "Ju",
  "license": "MIT",
  "dependencies": {
    "gsap": "^3.2.2",
    "parcel": "^2.0.0"
  }
}
